political_actions = {
	open_ledger = {
		icon = generic_research

		allowed = {
			always = yes
			is_ai = no
		}

		available = {
			NOT = { has_country_flag = pop_ledger_open }
		}

		visible = {
			always = yes
		}

		complete_effect = {
			set_country_flag = pop_ledger_open
		}
	}
}