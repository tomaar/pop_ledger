log_default = {
	every_state = { 
		set_variable = {
			var = pop_default
			value = state_population_k
		}
	}
}

load_states = {
	every_country = {
		every_owned_state = {
			add_to_array = {
				array = global.pop_array
				value = THIS
			}
		}
	}
}

pop_ledger_setup = {
	log_default = yes
	load_states = yes
}